<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=\, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full bg-zinc-900 text-white p-10">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/">Back to Home</a>
        </div>
        <h1 class="text-3xl tracking-lighter mb-3">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% users.forEach((user) => { %>
                <div class="user w-80 p-4 bg-zinc-800 rounded-lg text-center">
                    <div class="w-full h-72 rounded-lg bg-white overflow-hidden">
                        <img class="w-full h-full object-cover object-top" src="<%= user.image %>" alt="Image">
                    </div>
                    <h3 class="text-xl mt-3 tracking-lighter"><%= user.name %></h3>
                    <h5 class="text-zinc-500"><%= user.email %></h5>
                    <div class="flex mx-3 mt-10 justify-between">
                        <a class="text-yellow-500" href="">Edit user</a>
                        <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>